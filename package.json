{
  "name": "traffic-flow-ai",
  "version": "2.4.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && node ./node_modules/typescript/bin/tsc -b && node ./node_modules/vite/bin/vite.js build",
    "dev": "cd frontend && npm run dev"
  }
}
